site_name: "${{ values.title }}"
site_description: "${{ values.description }}"
repo_url: "https://dev.azure.com/${{ values.azureDevOpsOrg }}/${{ values.azureDevOpsProject }}/_git/${{ values.name }}"

docs_dir: docs

nav:
  - Home: index.md

plugins:
  - techdocs-core
