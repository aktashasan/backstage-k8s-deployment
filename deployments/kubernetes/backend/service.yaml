apiVersion: v1
kind: Service
metadata:
  name: backstage-backend
  namespace: backstage
spec:
  type: ClusterIP
  ports:
  - port: 7007
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: backstage-backend
